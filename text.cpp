##  内存泄露和内存溢出
1. 内存泄露：分配出去的内存无法收回（不再使用的对象或者变量仍然占内存空间）。
2. 内存溢出：程序要求的内存超出了系统所能分配的范围（栈满了还要入栈出现上溢，栈空了还要出栈出现下溢）。
##  内存泄露的的几种场景
1. 长生命周期的对象持有短生命周期对象的引用，即静态集合类。
2. 变量的不合理的作用域。
3. 各种连接器没显示关闭。
4. 监听器。
##  避免内存泄露
1. 尽早释放无用对象的引用。
2. 使用字符串处理，避免使用STRING，应大量使用StringBuffer，每一个String对象都得要独立占用内存的一块区域。